# Keep the build context (and image) lean — see Dockerfile.
.git
.github
.sandcastle
node_modules
docs
tests
*.md
!README.md

# Local secrets — never ship into the image.
.env
.env.*
!.env.example

# Python caches
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
